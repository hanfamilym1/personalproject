import React, {Component} from 'react'
import Nav from '../Nav'

export default class Settings extends Component {
    render(){
        return(
            <div>
                <Nav/>
                Settings Component
            </div>
        )
    }
}